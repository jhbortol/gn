name: Azure Static Web Apps CI/CD - Development

on:
  push:
    branches:
      - dev-hb

jobs:
  build_and_deploy_job:
    runs-on: ubuntu-latest
    name: Build and Deploy to Development
    permissions:
       id-token: write
       contents: read
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: true
          lfs: false
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'
      - name: Install dependencies
        run: npm ci
      - name: Build for Development
        run: npm run build
      - name: Debug - List build output
        run: ls -la dist/guia-noivas/browser/ || echo "Build output not found"
      - name: Deploy to Azure Static Web Apps
        id: builddeploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_BLACK_SMOKE_06AE9120F }}
          action: "upload"
          app_location: "/"
          api_location: ""
          output_location: "dist/guia-noivas/browser"
          skip_app_build: true
